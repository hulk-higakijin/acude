<div class="absolute w-screen top-20">
  <h1 class="font-bold text-4xl">Universities#show</h1>
  <span><%= @university.name %></span>
  <a href="<%= @university.url %>"><%= @university.url %></a>
  <br>
  <% @university.faculties.each do |faculty| %>
    <%= faculty.name %><br>
  <% end %>
</div>
